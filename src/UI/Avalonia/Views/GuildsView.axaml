<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="using:Volte.UI.Avalonia.ViewModels"
             xmlns:models="using:Volte.UI.Avalonia.Models"
             xmlns:pi="using:Projektanker.Icons.Avalonia"
             xmlns:controls="using:Volte.UI.Avalonia.Controls"
             xmlns:asyncImageLoader="clr-namespace:AsyncImageLoader;assembly=AsyncImageLoader.Avalonia"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Volte.UI.Avalonia.Views.GuildsView"
             x:DataType="viewModels:GuildsViewModel">
    <UserControl.DataContext>
        <viewModels:GuildsViewModel />
    </UserControl.DataContext>
    <Grid ColumnDefinitions="Auto,Auto,*">
        <ScrollViewer Grid.Column="0">
            <ListBox Name="GuildsList"
                     Margin="5"
                     SelectionMode="Toggle"
                     SelectedItem="{Binding SelectedGuild}"
                     ItemsSource="{Binding Guilds}">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="models:GuildModel">
                        <Grid>
                            <Border
                                Margin="0"
                                Padding="0, 5, 3, 5"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                ClipToBounds="True"
                                CornerRadius="5">
                                <StackPanel Orientation="Horizontal" Spacing="5" HorizontalAlignment="Left" VerticalAlignment="Top" >
                                    <controls:CircularImage Margin="0, 0, 5, 0" asyncImageLoader:ImageLoader.Source="{Binding SmallIconUrl}" MaxWidth="64" MaxHeight="64"/>
                                    <StackPanel Orientation="Vertical" Spacing="5" Margin="0, 5, 0, 0">
                                        <TextBlock
                                            HorizontalAlignment="Stretch"
                                            FontWeight="Bold"
                                            Text="{Binding Entity.Name}"
                                            TextAlignment="Start"
                                            TextWrapping="Wrap" />
                                        <TextBlock
                                            HorizontalAlignment="Stretch"
                                            TextAlignment="Start"
                                            TextWrapping="Wrap">
                                            <Run Text="Owner: "/>
                                            <Run Text="{Binding Entity.Owner.Username}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
        <!-- ReSharper disable Xaml.PossibleNullReferenceException -->
        <!-- Visibility binding Selected will cause this section of the control to not display at all when SelectedGuild is null, no fallback is required. -->
        <Border Grid.Column="1"
                Margin="0, 5"
                BorderBrush="DimGray" 
                BorderThickness="1" 
                VerticalAlignment="Stretch" 
                HorizontalAlignment="Stretch"
                CornerRadius="1"
                IsVisible="{Binding Selected}"/>
        <Grid Grid.Column="2" IsVisible="{Binding Selected}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto,*,Auto" VerticalAlignment="Top" Margin="5">
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Spacing="2" VerticalAlignment="Top">
                    <StackPanel Orientation="Horizontal" Spacing="10" Margin="5, 5, 0, 15">
                        <Border>
                            <controls:RoundedImage CornerRadius="16" asyncImageLoader:ImageLoader.Source="{Binding SelectedGuild.IconUrl}"/>
                        </Border>
                        <StackPanel Orientation="Vertical" Spacing="5">
                            <TextBlock
                                Padding="0, 15, 0, 0"
                                HorizontalAlignment="Stretch"
                                FontWeight="Bold"
                                Text="{Binding SelectedGuild.Entity.Name}"
                                FontSize="30"
                                TextAlignment="Start"
                                TextWrapping="Wrap" />
                            <TextBlock
                                HorizontalAlignment="Stretch"
                                TextAlignment="Start"
                                TextWrapping="Wrap">
                                <Run Text="ID: "/>
                                <Run Text="{Binding SelectedGuild.Entity.Id}"/>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock
                        HorizontalAlignment="Stretch"
                        TextAlignment="Start"
                        TextWrapping="Wrap">
                        <Run Text="Owned by: "/>
                        <Run Text="{Binding SelectedGuild.Entity.Owner.Username}"/>
                    </TextBlock>
                    <TextBlock
                        HorizontalAlignment="Stretch"
                        TextAlignment="Start"
                        TextWrapping="Wrap">
                        <Run Text="Members: "/>
                        <Run Text="{Binding SelectedGuild.Entity.MemberCount}"/>
                    </TextBlock>
                    <TextBlock
                        HorizontalAlignment="Stretch"
                        TextAlignment="Start"
                        TextWrapping="Wrap">
                        <Run Text="Channels: "/>
                        <Run Text="{Binding SelectedGuild.Entity.Channels.Count}"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="2" Orientation="Vertical" Spacing="5" VerticalAlignment="Top" Margin="15">
                    <Button
                        Name="LeaveSelectedGuildButton"
                        HorizontalAlignment="Right"
                        Background="DarkRed">
                        <StackPanel Orientation="Horizontal" Spacing="2">
                            <TextBlock 
                                Text="Leave"
                                FontWeight="Bold"
                                TextAlignment="Start"/>
                            <pi:Icon Value="mdi-door-open" />
                        </StackPanel>
                    </Button>
                    <Button
                        Name="DeleteSelectedGuildDataButton"
                        HorizontalAlignment="Right"
                        Background="Red">
                        <StackPanel Orientation="Horizontal" Spacing="2">
                            <TextBlock 
                                Text="Reset DB entry"
                                FontWeight="Bold"
                                TextAlignment="Start"/>
                            <pi:Icon Value="mdi-delete-forever" />
                        </StackPanel>
                    </Button>
                    <Button
                        Name="SendMessageToGuildOwnerButton"
                        HorizontalAlignment="Right">
                        <StackPanel Orientation="Horizontal" Spacing="2">
                            <TextBlock 
                                Text="Message Owner"
                                FontWeight="Bold"
                                TextAlignment="Start"/>
                            <pi:Icon Value="mdi-message" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
        <!-- ReSharper restore Xaml.PossibleNullReferenceException -->
    </Grid>
</UserControl>
